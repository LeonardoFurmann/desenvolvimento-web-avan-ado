import { useParams } from "react-router-dom"


export default function Sobre(){
   // const { name } = useParams()
    return (
        <div className="Conteiner">
            {/* <p>Página Sobre {name}</p> */}
        </div>
    )
}